# Agent notes

Mac/Linux Docker: run Spark from the Jupyter container, not a host `pyspark`.

Portable Spark skills (performance tuning + debugging) live in `skills/`. They are independent of the course notebooks. Attach them when an agent should optimize or debug Spark from the UI; leave them off for a baseline eval.
